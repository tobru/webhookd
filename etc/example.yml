---
global:
  loglevel: 'debug'
  logfile: 'app.log'
  username: 'deployer'
  password: 'Deploy1T'
vcs:
  puppet-control:
    _all:
      command: 'echo this is <%= branch_name %>'
    production:
      command: 'echo this is production'
    nocommandbranch:
  encdata:
    master:
      command: 'git pull'

