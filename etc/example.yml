---
global:
  loglevel: 'debug'
  logfile: 'app.log'
  logfile_errors: 'error.log'
  username: 'deployer'
  password: 'Deploy1T'
vcs:
  puppet-control:
    _all:
      command: 'echo _all'
    production:
      #command: 'r10k deploy environment <%= branch_name %> -pv'
      command: 'echo this is <%= branch_name %>'
    nocommandbranch:
  encdata:
    master:
      command: 'git pull'

